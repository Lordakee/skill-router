# 全项目审核：skill-router（GitHub开源版）

审核对象：当前工作区 D:/Projects/skill-router/ 全部代码（已推送到 github.com/Lordakee/skill-router main分支）。

## 项目背景
跨agent（ZCode、Codex CLI、Claude Code、OpenCode）的skill路由系统：全文入只读vault、扫描根stub化、单一MCP server提供skill_discover、skill_load、skill_health。setup.mjs一键安装（快照→迁移→stub→四agent注册），rollback.mjs字节精确回滚，uninstall.mjs卸载。零npm依赖。核心代码经过生产验证+此前三轮终审（APPROVED）。

## 审核维度
1. **正确性**：setup、rollback、uninstall三工具逻辑；migrate与regenerate依赖链；边界条件（空roots、重复id、符号链接根、Windows与POSIX路径差异）
2. **可移植性**：MSYS tar路径转换（posix函数）在真实Windows与CI ubuntu上的行为；TOML块删除逻辑（codex deregister）对非我们追加的同类section的误伤风险
3. **安全**：密钥与个人信息泄露（开源要求）；server.js路径逃逸防护是否被setup、uninstall引入的新面破坏
4. **代码质量**：API一致性、错误处理、dry-run覆盖完整性（uninstall的dry-run是否真的零写入）
5. **文档**：README与实际行为一致性；CI workflow正确性
6. **可维护性**：setup与uninstall间重复的restoreRoots逻辑是否值得提取

## 已知沙箱测试记录
- round-trip: setup→rollback 字节精确（beta双根变体各自还原）
- lifecycle: setup→uninstall(--snapshot --purge-vault) 0 stub残留、TOML合法
- tests目录run-tests.js PASS

## 输出格式
- 总裁定: APPROVED 或 NEEDS_FIXES
- 发现按 P0(必须修)、P1(建议修)、P2(可选) 分级，每条给文件+行号+问题+修复建议
- 不要修改任何文件，只输出审核报告
